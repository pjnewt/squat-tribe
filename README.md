Squat Tribe v1.3

Upload all files in this zip to the root of your GitHub Pages repository.

If you previously deployed an older version, remove the old files first.
Then upload these files and wait for GitHub Pages to refresh.

On Android Chrome:
1. Open the page once.
2. Refresh once.
3. Use the Chrome menu to install, or tap the in-app Install App button if it appears.

If an old cached version still appears, open Chrome site settings for the page and clear storage, then reload.
